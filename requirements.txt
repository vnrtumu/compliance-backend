# Compliance Backend - Python Dependencies
# =========================================

# FastAPI and Web Framework
fastapi==0.128.0 (this is for web framework)
uvicorn==0.39.0 (this is for server)
python-multipart==0.0.20 (this is for file uploads)
starlette==0.49.3 (this is for web framework)

# Database
SQLAlchemy==2.0.45 (this is ORM)
psycopg2-binary==2.9.11 (this is driver)
alembic==1.16.5 (this is migration tool)

# LLM Providers
openai==2.15.0 (this is for openai)
groq==0.37.1 (this is for groq)

# Data Validation
pydantic==2.12.5 (this is a validator)
pydantic-settings==2.11.0 (this is for settings)
email-validator==2.3.0 (this is for email validation)

# PDF Processing
PyMuPDF==1.26.5 (this is for pdf processing)

# HTTP Client
requests==2.32.5 (this is for http client)
httpx==0.28.1 (this is for http client)

# Environment and Config
python-dotenv==1.2.1 (this is for environment variables)

# Utilities
tiktoken==0.12.0 (this is for tokenization)
tenacity==9.1.2 (this is for retry logic)
PyYAML==6.0.3 (this is for yaml processing)
